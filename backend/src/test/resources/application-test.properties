# Profil « test » : complète application.properties (au lieu de le remplacer).
# Base en mémoire, indépendante de toute base locale (B6), sans données de démonstration.
spring.datasource.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
spring.flyway.locations=classpath:db/migration
